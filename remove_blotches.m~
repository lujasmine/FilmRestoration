function corrected = remove_blotches(imgs)    
%     blotches_marked = detect_blotches(imgs, 1, 256);
%     corrected = correct_blotches(blotches_marked, imgs);
    corrected = detect_blotches(imgs, 33, 34);
%     corrected = correct_blotches(imgs, blotches);
    
%     disp(blotches2(:,:,34));
    
    figure
    subplot(1,3,1)
    imshow(imgs(:,:,34))
    subplot(1,3,2)
    imshow(imgs(:,:,34))
    subplot(1,3,3)
    imshow(corrected(:,:,34))

%     disp(blotches(:,:,34))
    
end

% S-ROD algorithm for blotch detection as outlined in 'biemond1.pdf'
function corrected = detect_blotches(imgs, start_frame, end_frame)

%     blotches = zeros(size(imgs));
    corrected = imgs;
    T1 = 0;
    
    for n = start_frame+1 : end_frame
        for r = 2 : size(imgs,1)-1
           for col = 1 : size(imgs,2)
               
               if ()
               
           end
        end   
    end
end